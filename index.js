// n8n loads nodes and credentials from the paths declared in the `n8n` block of
// package.json, not from this file. It exists so that `require('n8n-nodes-whatsapp-advanced')`
// resolves rather than throwing, and so the pure helpers can be reused directly.
module.exports = {};
